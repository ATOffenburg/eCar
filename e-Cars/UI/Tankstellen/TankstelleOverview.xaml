<UserControl x:Class="e_Cars.UI.Tankstellen.TankstelleOverview"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             Style="{StaticResource UserControlStyle}"
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>

        <StackPanel Margin="0" Grid.Row="0" Orientation="Horizontal">
            <Button x:Name="ButtonBack" Content="_Zurück" Click="ButtonBack_Click" Width="60"/>
            <Button x:Name="ButtonCreateNew" Content="_Neu anlegen..." Click="ButtonCreateNew_Click" Width="100"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Label Margin="10,8,0,7" HorizontalAlignment="Left"  >Filter:</Label>
            <TextBox x:Name="TextBoxFilter" TextAlignment="Left" VerticalAlignment="Top" Margin="54,12,10,0" Height="18"/>
        </Grid>
        <Grid Grid.Row="3">
            <Border Margin="5" BorderBrush="Black" BorderThickness="1">
                <ListView x:Name="myListView" ItemsSource="{Binding listTankstelleInfo}" MouseDoubleClick="myListView_MouseDoubleClick">

                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" Width="Auto" DisplayMemberBinding="{Binding ID}"></GridViewColumn>
                            <GridViewColumn Header="Name" Width="Auto" DisplayMemberBinding="{Binding Name}"></GridViewColumn>
                            <GridViewColumn Header="Strasse" Width="Auto" DisplayMemberBinding="{Binding Strasse}"></GridViewColumn>
                            <GridViewColumn Header="PLZ" Width="Auto" DisplayMemberBinding="{Binding PLZ}"></GridViewColumn>
                            <GridViewColumn Header="Ort" Width="Auto" DisplayMemberBinding="{Binding Ort}"></GridViewColumn>
                            <GridViewColumn Header="Breitengrad" Width="Auto" DisplayMemberBinding="{Binding breitengrad}"></GridViewColumn>
                            <GridViewColumn Header="Längengrad" Width="Auto" DisplayMemberBinding="{Binding laengengrad}"></GridViewColumn>
                        </GridView>

                    </ListView.View>
                </ListView>
            </Border>
        </Grid>
    </Grid>
</UserControl>
